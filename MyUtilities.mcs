
class MiProgramaPrueba
{
    void Main()
    {
        int a;
        int b;
        int resultado;

        a = 10;
        // write(a); // Comentado o eliminado

        b = 5;
        // write(b); // Comentado o eliminado

        resultado = a - b; 
        write(resultado);    // Solo este write se ejecutará y mostrará 50
    }
}