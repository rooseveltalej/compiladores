class MiProgramaPrueba
{
    void Main()
    {
        int a;
        int b;
        int resultado; 

        a = 10;
        // write(a); 

        b = 5;
        // write(b); 

        resultado = a - b; 
        write(resultado);    
    }
}