
class MiProgramaPrueba
{
    void Main()
    {
        int a;
        int b;
        int resultado;

        a = 10;
        write(a); // Debería funcionar ahora si VisitDesignatorFactor carga locales

        b = 5;
        write(b);

        resultado = a * b; 
        write(resultado);    // Debería ser 15

     
    }
}