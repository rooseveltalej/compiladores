// testObjects.mcs
// testObjects.mcs
class TestObjects { // Esta es la clase que coincide con la regla 'program'
    class Point {   // Point es ahora una CLASE ANIDADA
        int x;
        int y;
    }

    Point globalP; 

    void Main() {
        Point localP;   
        // ... el resto de tu código Main
        write("Probando creacion de objeto localP...");
        localP = new Point(); 
        write("localP creado.");

        // ... etc ...
    }
}